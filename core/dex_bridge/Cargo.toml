[package]
name = "apex_dex_bridge"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
name = "apex_dex_bridge"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.29.0", features = ["extension-module"] }
apex_dex_parser = { path = "../dex_parser" }
