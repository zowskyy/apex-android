[build-system]
requires = ["maturin>=1.14,<2.0"]
build-backend = "maturin"

[project]
name = "apex-zip-reader"
version = "0.1.0"
requires-python = ">=3.9"

[tool.maturin]
bindings = "pyo3"
module-name = "apex_zip_reader"
manifest-path = "Cargo.toml"
